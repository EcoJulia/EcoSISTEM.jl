name: CI

on:
  push:
    branches:
      - main
      - dev
    tags:
      - 'v*'
  pull_request:

permissions: # needed to allow julia-actions/cache to proactively delete old caches that it has created
  actions: write
  contents: read

jobs:
  EcoSISTEM-tests:
    name: Julia ${{ matrix.julia-version }} - ${{ matrix.os }} - ${{ matrix.arch }} - ${{ github.event_name }}
    timeout-minutes: 60
    runs-on: ${{ matrix.os }}
    permissions: # needed to allow julia-actions/cache to proactively delete old caches that it has created
      actions: write
      contents: read
    strategy:
      matrix:
        julia-version:
          - '1.11'
          - '1'
        os:
          - ubuntu-latest
          - macOS-latest
          - windows-latest
        R-version:
          - 'release'
        arch:
          - x64
          - arm64
        experimental:
          - false
        exclude:
          - os: macOS-latest
            arch: x64
          - os: ubuntu-latest
            arch: arm64
          - os: windows-latest
            arch: arm64
      fail-fast: false
    steps:
    - name: Checkout code
      uses: actions/checkout@v7
    - name: Set up julia
      uses: julia-actions/setup-julia@v3
      with:
        version: ${{ matrix.julia-version }}
        arch: ${{ matrix.arch }}
    - name: Cache
      uses: julia-actions/cache@v3
    - name: Cache raster data
      # julia-actions/cache caches the package depot but not scratchspaces, where the raster
      # downloads (~850 MB) and aggregated layers (~700 MB) live. Cache them here (tar preserves
      # mtime, so the aggregate cache — keyed partly on source mtime — still hits). Keyed on every
      # file that actually triggers a download (getraster/read/readCRUTS/readCHELSA_monthly/download
      # calls — not just the two reader test files), with a prefix fallback so the newest is restored
      # otherwise: a restored cache can never be wrong, only incomplete, since getraster/download
      # each check the file's own existence before fetching — a stale/partial restore just costs a
      # few more per-file downloads, not incorrect data. Aggregate correctness across code changes is
      # handled in-code via the cache key's file-content hash, so this key need not track
      # src/ClimatePref/ReadData.jl.
      uses: actions/cache@v6
      with:
        path: ~/.julia/scratchspaces
        key: rasterdata-${{ runner.os }}-${{ hashFiles('test/test_ReadData.jl', 'test/test_AbioticEnv.jl', 'test/test_deprecations.jl', 'examples/ScottishCultivatedLand.jl', 'notebooks/InteractiveAfrica.jl') }}
        restore-keys: |
          rasterdata-${{ runner.os }}-
    - name: Build package
      uses: julia-actions/julia-buildpkg@v1
    - name: Running
      # ⭐ Coverage instrumentation is a large slowdown and only one job's output is ever used, so it
      # runs on exactly one matrix entry — macOS, which is the fastest runner here, on the release
      # Julia. Every other entry runs the same tests uninstrumented.
      uses: julia-actions/julia-runtest@v1
      with:
        coverage: ${{ matrix.os == 'macOS-latest' && matrix.julia-version == '1' }}
    - name: Discard the extracted archives before the cache is saved
      # ⭐ Worth 182 MB of the ~1 GB raster cache, at no cost. These are the *downloaded archives*,
      # and RasterDataSources extracts each one and thereafter tests `!isfile(raster_path)` on the
      # **extracted** tif before fetching anything (`WorldClim/climate.jl:21`) — so the archive is
      # only ever consulted when its tif is missing, and caching the tifs without it triggers no
      # re-download.
      #
      # ⚠️ Done as a step rather than as a `!`-exclusion in the cache `path`, because
      # `actions/cache` saves in a post-job step: deleting here is unambiguous, whereas whether a
      # leading `~` expands inside an exclusion pattern is not.
      if: always()
      shell: bash
      run: rm -rf ~/.julia/scratchspaces/*/assets/RasterDataSources/*/*/zips
    - name: Process coverage
      if: matrix.os == 'macOS-latest' && matrix.julia-version == '1'
      uses: julia-actions/julia-processcoverage@v1
    - name: Codecov
      if: matrix.os == 'macOS-latest' && matrix.julia-version == '1'
      uses: codecov/codecov-action@v7
      with:
        token: ${{ secrets.CODECOV_TOKEN }}
        verbose: true
