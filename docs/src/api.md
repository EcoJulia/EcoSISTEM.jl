```@meta
CurrentModule = EcoSISTEM
```

# API

```@index
```

**No extension is in this list.** Every public name whose implementation lives in an extension
keeps its docstring on a method-less stub in the parent, so the parent's modules document
everything.

```@autodocs
Modules = [EcoSISTEM, EcoSISTEM.ClimatePref, EcoSISTEM.Units]
Private = true
```

```@index
```
